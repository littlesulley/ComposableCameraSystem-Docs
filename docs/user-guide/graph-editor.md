# The Graph Editor

!!! info "Under construction"
    This page is being written.

The bespoke `UEdGraph` used to author camera types. Pins, nodes, parameters, variables, and the round-trip between graph and runtime data.
