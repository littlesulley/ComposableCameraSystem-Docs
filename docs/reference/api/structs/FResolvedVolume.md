
# FResolvedVolume { #fresolvedvolume }

Resolved volume packed for the debug draw + clamp logic. All fields populated in-tick; not persisted across ticks (node is otherwise stateless and scrubbable).

### Public Attributes

| Return | Name | Description |
|--------|------|-------------|
| `EComposableCameraVolumeShape` | [`Shape`](#shape-1)  |  |
| `FVector` | [`Center`](#center)  |  |
| `FRotator` | [`Rotation`](#rotation-3)  |  |
| `FVector` | [`BoxExtents`](#boxextents-1)  |  |
| `float` | [`SphereRadius`](#sphereradius-1)  |  |

---

#### Shape { #shape-1 }

```cpp
EComposableCameraVolumeShape Shape = 
```

---

#### Center { #center }

```cpp
FVector Center = FVector::ZeroVector
```

---

#### Rotation { #rotation-3 }

```cpp
FRotator Rotation = FRotator::ZeroRotator
```

---

#### BoxExtents { #boxextents-1 }

```cpp
FVector BoxExtents = FVector::ZeroVector
```

---

#### SphereRadius { #sphereradius-1 }

```cpp
float SphereRadius = 0.f
```

## FResolvedVolume { #fresolvedvolume }

Resolved volume packed for the debug draw + clamp logic. All fields populated in-tick; not persisted across ticks (node is otherwise stateless and scrubbable).

### Public Attributes

| Return | Name | Description |
|--------|------|-------------|
| `EComposableCameraVolumeShape` | [`Shape`](#shape-1)  |  |
| `FVector` | [`Center`](#center)  |  |
| `FRotator` | [`Rotation`](#rotation-3)  |  |
| `FVector` | [`BoxExtents`](#boxextents-1)  |  |
| `float` | [`SphereRadius`](#sphereradius-1)  |  |

---

#### Shape { #shape-1 }

```cpp
EComposableCameraVolumeShape Shape = 
```

---

#### Center { #center }

```cpp
FVector Center = FVector::ZeroVector
```

---

#### Rotation { #rotation-3 }

```cpp
FRotator Rotation = FRotator::ZeroRotator
```

---

#### BoxExtents { #boxextents-1 }

```cpp
FVector BoxExtents = FVector::ZeroVector
```

---

#### SphereRadius { #sphereradius-1 }

```cpp
float SphereRadius = 0.f
```
